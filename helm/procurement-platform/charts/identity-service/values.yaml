replicaCount: 1

image:
  repository: ""
  pullPolicy: IfNotPresent
  tag: "latest"

serviceAccount:
  create: true
  annotations: {}
  name: ""

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false

service:
  type: ClusterIP
  port: 5001
  targetPort: 5001

healthCheck:
  path: /api/health

ingress:
  enabled: true
  className: alb
  groupOrder: 10
  pathPrefixes:
    - path: /api/auth
      pathType: Prefix
    - path: /api/users
      pathType: Prefix
    - path: /api/hr
      pathType: Prefix

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

pdb:
  enabled: true
  minAvailable: 1

extraEnv: {}
